# Hello WASM

A demo module installed at runtime: a sandboxed WASM backend plus a Module Federation page, with no server rebuild.

Layout: `server/` (Rust backend), `ui/` (frontend), `module.json` (manifest). See `modules/README.md` for the module authoring guide.
