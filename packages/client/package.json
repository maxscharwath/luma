{
  "name": "@luma/client",
  "version": "0.1.3",
  "type": "module",
  "description": "LUMA server client: the typed API client (LumaClient), ts-rs wire types, zod runtime schemas + branded ids, and session storage. Shared by every LUMA app.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsgo --noEmit",
    "build": "tsgo --noEmit"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
