{
  "name": "@luma/admin-kit",
  "version": "0.1.0",
  "type": "module",
  "description": "LUMA admin UI contract: the presentational primitives, hooks, and host-context provider that admin pages (built-in AND module-contributed) render with. Shared by @luma/web and every module ui/ package so a module page never imports app internals.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsgo --noEmit",
    "build": "tsgo --noEmit"
  },
  "dependencies": {
    "@luma/core": "workspace:*",
    "@luma/ui": "workspace:*",
    "@radix-ui/react-select": "^2",
    "@tabler/icons-react": "^3.44.0",
    "@tanstack/react-query": "^5.101.2"
  },
  "peerDependencies": {
    "react": ">=18",
    "react-dom": ">=18"
  },
  "devDependencies": {
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "typescript": "^7.0.2"
  }
}
