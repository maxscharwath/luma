{
  "name": "@luma/synology-repo",
  "version": "0.1.4",
  "type": "module",
  "private": true,
  "description": "Generate a Synology package-source repository (catalog.json + landing page) from a built .spk, for static hosting (GitHub Pages, Cloudflare Pages, any static host).",
  "bin": {
    "luma-synology-repo": "./src/gen-catalog.ts"
  },
  "scripts": {
    "gen": "bun run src/gen-catalog.ts",
    "preview": "bun run src/preview.ts",
    "typecheck": "tsgo --noEmit",
    "build": "tsgo --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.0"
  }
}
