# Rust / Tauri build output
/target
# Tauri-generated schemas + code (regenerated by the CLI on build)
/gen
# Windows libmpv fetched/generated at build time (scripts/fetch-libmpv-windows.ps1):
# the mpv-dev download + synthesised mpv.lib, and the DLL staged for bundling.
/.libmpv-win
/libmpv-2.dll
# Linux mpv sidecar binary (fetched by scripts/fetch-mpv.sh; large, per-arch)
/bin
