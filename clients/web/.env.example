# Override the KROMA server origin at build/dev time.
# If unset, the app defaults to http://<current-host>:4040 and can also be
# changed at runtime from the in-app connection screen.
VITE_KROMA_SERVER=http://localhost:4040
