# Release parser

Scene / P2P release-name parsing and scoring for the acquisition stack.

Layout: `server/` (Rust backend), `module.json` (manifest). See `modules/README.md` for the module authoring guide.
